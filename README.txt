Spring Web Services example using JAXB for marshalling/unmarshalling.

This project can be built using Maven 

It can also be run from the command line with 'mvn tomcat:run' ('http://localhost:8080/ws') 
and then the client test can be run.

