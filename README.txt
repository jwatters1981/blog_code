Spring Web Services example using JAXB for marshalling/unmarshalling.

This project can be built using Maven 
mvn install tomcat:run

It can also be run from the command line with 'mvn tomcat:run' ('http://localhost:8080/spring-web-services/productService.wsdl') 


